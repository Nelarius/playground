
class Float64 {
    // Returns the largest finite value
    foreign static max

    // Returns the smallest finite value
    foreign static min
    
    // Returns the lowest finite value
    foreign static lowest

    // Returns the difference between 1.0 and the next representable value
    foreign static epsilon
}
